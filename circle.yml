machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.docker"
  override:
    - bin/circle-docker-provision.sh elasticsearch iojs selenium/standalone-chrome
    - ~/.docker/docker-compose run web npm install

test:
  override:
    - ~/.docker/docker-compose run web npm test
